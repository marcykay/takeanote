// Materialize CSS JQUERY COMPONENTS
$(document).ready(function() {
    $('.tooltipped').tooltip(); //tooltips
    $('#content1').trigger('autoresize');
    $('textarea#title1').characterCounter();
});
